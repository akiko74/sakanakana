  <div class="slideshow">
    <% @pictures.each do |picture| %>
    <% @detail = picture.details.first %>
      <tr>
       <td><%= link_to (image_tag picture.image.url(:medium)), detail_path(@detail, :picture_id => picture.id) %></td>
      </tr>
    <% end %>
  </div>
