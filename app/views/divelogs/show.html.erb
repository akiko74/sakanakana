<script type="text/javascript">
$(function(){
  var $container = $('.slideshow').cycle({
    speed: 300,
    autostop : 1,
    autostopCount: 1
 });

  var obj = $('#thumbnail img');
  $container.children().each(function(i){
    $(obj[i]).css('cursor', 'pointer').click(function(){
        $container.cycle(i);
        return false;
    });
  });
});
</script>

<p id="notice"><%= notice %></p>

<p>
  <b>ダイビングの日付:</b>
  <%= @divelog.logdate %>
  <b>ポイント:</b>
  <%= @divelog.divesite.pointname %>
</p>

  <div class="slideshow")
    <% @pictures.each do |picture| %>
      <tr>
       <td><%= image_tag picture.image.url(:medium) %></td>
      </tr>
    <% end %>
  </div>
  <div id="thumbnail">
    <% @pictures.each do |picture| %>
      <tr>
       <td><%= image_tag picture.image.url(:thumb) %></td>
      </tr>
    <% end %>
   </div>

<% @details.each do |detail| %>
<% @picture = detail.pictures.first %>
 <tr>
   <td><%#= link_to detail.name, detail_path(:id => detail.id) %></td>
   <td><%= link_to detail.name, picture_path(:id => @picture.id, :detail_id => detail.id) %></td>
 <% end %>
 </tr>

<%= link_to 'Edit', edit_divelog_path(@divelog) %> |
<%= link_to 'Back', divelogs_path %>
